[build]
pythonVersion = "3.11"
installCommand = "pip install --upgrade pip && pip install ."

[scripts]
start = "redbot-setup instance create --name railway --data-dir /app/data --token $DISCORD_TOKEN && redbot railway"
